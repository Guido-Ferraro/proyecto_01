import React from "react";

const DonattionInfo = ():React.ReactElement => {
	return (
		<i onClick={() => alert("Placeholder link")} className="info" >Why give $50?</i>
	);
};

export default DonattionInfo;